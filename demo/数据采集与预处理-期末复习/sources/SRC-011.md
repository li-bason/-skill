## Slide 1
数据清洗-pandas处理基础

## Slide 2
理解pandas中数据读写、选择、整理和描述的函数使用
掌握pandas数据选择的方法
理解数据分组、分割、合并和变形的相关函数使用
掌握pandas数据合并的方法



教学目标

## Slide 3
1
数据分组与分割
数据清洗-pandas处理基础
数据读写、选择、整理和描述
2
3
数据合并和变形

## Slide 4
数据读写、选择、整理和描述
Pandas是一个强大的分析结构化数据的工具集；提供数据清洗功能，同时也用于数据挖掘和数据分析。
利器之一：Series
它是一种类似于一维数组的对象，是由一组数据(各种NumPy数据类型)以及一组与之相关的数据标签(即索引)组成。仅由一组数据也可产生简单的Series对象。
利器之二：DataFrame
DataFrame是Pandas中的一个表格型的数据结构，包含有一组有序的列，每列可以是不同的值类型(数值、字符串、布尔型等)，DataFrame即有行索引也有列索引，可以被看做是由Series组成的字典。
安装Pandas需要很多依赖的库，安装起来比较麻烦，建议使用Anancoda，Anancoda内置了有关数据清洗和算法的库。



## Slide 5
数据读写、选择、整理和描述
1、从CSV中读取数据
Pandas中的read_csv()函数用于读取文本文件，具体的语法如下：
参数解释：filepath_or_buffer：文件类型；              sep：str类型，分隔符，默认的 '  ,  ' 分隔符；
                  delimiter：str类型，sep的替代参数，默认为Nonwe;
                  header：设定列名，默认值为'infer'，当选择默认值或header=0时，将首行设为列名。如果列名被传入明确值就令header=None；
                 name：列名列表的使用，默认值为 'infer' ，如果文件不包含列名，那么应该设置header=None。 列名列表中不允许有重复值；
                index_col：DataFrame的行索引列表, 既可以是字符串名称也可以是列索引. 如果传入一个字符串序列或者整数序列,那么一定要使用多级索引；
                 usecols：返回列名列表的子集。如果该参数为列表形式, 那么所有元素应全为位置或者 全为相应列的列名字符串。
pandas.read_csv(filepath_or_buffer, sep=',', delimiter=None, header='infer',names=None, index_col=None, usecols=None)

## Slide 6
数据读写、选择、整理和描述
1、从CSV中读取数据
读取数据，具体的代码如下：



代码运行结果如下：




import pandas as pd
#从csv中读取数据，还可以读取html、txt等格式的文件
df=pd.read_csv("F:/taobao_data.csv")
print(df) 

## Slide 7
数据读写、选择、整理和描述
2、从CSV中写入数据
Pandas中的to_csv()函数是DataFrame类的方法，向CSV中写入数据，具体语法如下：


参数解释：path_or_buf：路径或对象，如果没有提供，结果将返回为字符串；              
                   sep：输出文件的字段分隔符，默认的 '  ,  ' 分隔符；
                  delimiter：str类型，默认为 ' ' ，浮点数格式字符串;
                 float_format：str类型，默认为None，浮点数格式字符串；
                 columns：sequence类型，写入可选列；
                header：boolean或 str类型，默认为True；写出列名。如果给定字符串列表，则假定为列名的别名；
                index：boolean类型，写入行名称（索引）；
                index_label：string 或sequence,或 False, default None，如果需要，可以使用索引列的列标签。如果没有给出，且标题和索引为True，则使用索引名称。如果数据文件使用多索引，则应该使用这个序列。如果值为False，不打印索引字段。
DataFrame.to_csv(path_or_buf=None, sep=', ', na_rep='', float_format=None, columns=None, 
header=True, index=True, index_label=None)

## Slide 8
数据读写、选择、整理和描述
2、从CSV中写入数据
向CSV写入数据，具体的代码如下：




程序运行完成后会在相应目录（此处是：F:/）下生成taobao_price_data_csv文件，文件内容如下：



import pandas as pd
#从csv中读取数据，还可以读取html、txt等格式的文件
df=pd.read_csv("F:/taobao_data.csv")
df.to_csv("F:/taobao_price_data.csv",columns=['商品','价格'],index=False,header=True)

## Slide 9
数据读写、选择、整理和描述
3、选择数据
（1）行的选取
使用Pandas切片可以获取某些特定行数据。
获取前3行的数据，具体的代码如下：




选择第0行至第2行的数据，具体结果如下：

import pandas as pd
#从csv中读取数据，还可以读取html、txt等格式的文件
df=pd.read_csv("F:/taobao_data.csv")
rows=df[0:3]
print(rows)

## Slide 10
数据读写、选择、整理和描述
3、选择数据
（2）列的选取
使用Pandas 切片可以获取某些特定列数据。
获取宝贝、价格两列数据，具体的代码如下：





选择宝贝、价格两列数据，具体结果如下：


import pandas as pd
#从csv中读取数据，还可以读取html、txt等格式的文件
df=pd.read_csv("F:/taobao_data.csv")
cols=df[['宝贝','价格']]
print(cols)

## Slide 11
数据读写、选择、整理和描述
3、选择数据
（3）块的选取
使用Pandas 切片可以获取某些特定行和列数据。
获取前3行宝贝、价格两列数据，具体的代码如下：





运行程序，具体结果如下：


import pandas as pd
#从csv中读取数据，还可以读取html、txt等格式的文件
df=pd.read_csv("F:/taobao_data.csv")
rcols=df.ix[0:2,['宝贝','价格']]
print(rcols)

## Slide 12
数据读写、选择、整理和描述
3、选择数据
（4）操作行和块
使用Pandas series实现对行和列的操作。
从已有的列中创建一个新的列，具体的代码如下：





运行程序，具体结果如下：


import pandas as pd
#从csv中读取数据，还可以读取html、txt等格式的文件
df=pd.read_csv("F:/taobao_data.csv")
df['销售量']=df['价格']*df['成交量']
print(df)

## Slide 13
数据读写、选择、整理和描述
3、选择数据
（5）过滤行
使用Pandas 过滤实现对行数据的过滤。
选择价格<100并且成交量>10000的所有数据，具体的代码如下：





运行程序，具体结果如下：


import pandas as pd
#从csv中读取数据，还可以读取html、txt等格式的文件
df=pd.read_csv("F:/taobao_data.csv")
data=df[(df['价格']<100)&(df['成交量']>10000)]
print(data)

## Slide 14
数据读写、选择、整理和描述
3、选择数据
在Dataframe中选取数据大抵包括3中情况：
行(列)选取(单维度选取)：df[]。这种情况一次只能选取行或者列，即一次选取中，只能为行或者列设置筛选条件(只能为一个维度设置筛选条件)。
区域选取(多维选取)：df.loc[]，df.iloc[]，df.ix[]。这种方式可以同时为多个维度设置筛选条件。
df.loc[ ]只能使用标签索引，不能使用整数索引，通过便签索引切边进行筛选时，前闭后闭。
df.iloc[]只能使用整数索引，不能使用标签索引，通过整数索引切边进行筛选时，前闭后开。
df.ix[]既可以使用标签索引，也可以使用整数索引。
单元格选取(点选取)：df.at[]，df.iat[]。准确定位一个单元格。
df.at[]和df.iat[]使用时必须输入两个参数，即行索引和列索引，其中df.at[]只能使用标签索引，df.iat[]只能使用整数索引。df.at[]和df.iat[]选取的都是单个单元格(单行单列)，所以返回值都为基本数据类型。


## Slide 15
课堂练习
import pandas as pdimport numpy as npdata = {'name': ['Joe', 'Mike', 'Jack', 'Rose', 'David', 'Marry', 'Wansi', 'Sidy', 'Jason', 'Even'], 'age': [25, 32, 18, np.nan, 15, 20, 41, np.nan, 37, 32], 'gender': [1, 0, 1, 1, 0, 1, 0, 0, 1, 0], 'isMarried': ['yes', 'yes', 'no', 'yes', 'no', 'no', 'no', 'yes', 'no', 'no']}labels = ['a', 'b', 'c', 'd', 'e', 'f', 'g', 'h', 'i', 'j']df = pd.DataFrame(data, index=labels)
df['a':'b'])
df[['name','age']]df[[True,True,True,False,False,False,False,False,False,False]]df.loc['a', :]df.loc[[True,True,True,False,False,False], :]df.loc[df['age']>30,:]df.loc[df['age'] > 30, ['name', 'age']]
df.iloc[:2]
df.iloc[:, 1]df.iloc[[1,3,5],:]
df.iloc[:,[True,True,True,False]]
df.ix[['a','c'], [0,1,3]]
df.ix[df['isMarried']=='no',['name','age']]
df.at['b','name']

## Slide 16
数据读写、选择、整理和描述
4、整理数据
pandas.sort_index()函数实现：按标签对数据进行排序。
将数据按位置字段进行排序，具体的代码如下：






运行程序，具体结果如下：


import pandas as pd
#从csv中读取数据，还可以读取html、txt等格式的文件
df=pd.read_csv("F:/taobao_data.csv")
#将某个字段设置为index
df1=df.set_index('位置')
df1=df1.sort_index()
print(df1)

## Slide 17
数据读写、选择、整理和描述
4、整理数据
将数据按位置和卖家两个字段进行排序，其中位置为第一个排序因子，具体的代码如下：




运行程序，具体结果如下：


import pandas as pd
#从csv中读取数据，还可以读取html、txt等格式的文件
df=pd.read_csv("F:/taobao_data.csv")
df2=df.set_index(['位置','卖家'])
df2=df2.sort_index()
print(df2)

## Slide 18
数据读写、选择、整理和描述
4、整理数据
pandas.sort_values()函数实现：将数据集依照某个字段中的数据进行排序，该函数即可根据指定列/行数据排序。
将数据按成交量按位置分组计算求和，具体的代码如下：







运行程序，具体结果如下：


import pandas as pd
#从csv中读取数据，还可以读取html、txt等格式的文件
df=pd.read_csv("F:/taobao_data.csv")
#drop(默认axis=0)是删除行，axis=1是删除列
df_sum=df.drop(['宝贝','卖家'],axis=1).groupby('位置').sum()
df_sum=df_sum.sort_values('成交量',ascending=False)
print(df_sum)

## Slide 19
数据读写、选择、整理和描述
5、数据描述
为了快速了解数据的信息，使用一些指令。
pandas.info()函数可以查看表的数据信息，具体的代码如下：





运行程序，具体结果如下：


import pandas as pd
#从csv中读取数据，还可以读取html、txt等格式的文件
df=pd.read_csv("F:/taobao_data.csv")
#查看表的数据信息
df.info()

## Slide 20
数据读写、选择、整理和描述
5、数据描述
为了快速了解数据的信息，使用一些指令。
pandas.describe()函数可以查看表的描述性统计信息，具体的代码如下：






运行程序，具体结果如下：


import pandas as pd
#从csv中读取数据，还可以读取html、txt等格式的文件
df=pd.read_csv("F:/taobao_data.csv")
#查看表的描述性统计信息
des=df.describe()
print(des)

## Slide 21
1
数据分组与分割
数据清洗-pandas处理基础
数据读写、选择、整理和描述
2
3
数据合并和变形

## Slide 22
1、数据分组
（1）汇总数据
Pandas中的mean()函数可实现：指定字段作为索引、汇总数据。
将数据按位置进行分组，并计算成交量列的平均值，使用groupby进行分组，具体代码如下：






运行程序，具体结果如下：


import pandas as pd
#从csv中读取数据，还可以读取html、txt等格式的文件
df=pd.read_csv("F:/taobao_data.csv")
grouped=df['成交量'].groupby(df['位置'])
grouped=grouped.mean()
print(grouped)
数据分组与分割

## Slide 23
数据分组与分割
1、数据分组
将数据按多列进行分组，并计算成交量列的平均值，具体代码如下：






运行程序，具体结果如下：


import pandas as pd
#从csv中读取数据，还可以读取html、txt等格式的文件
df=pd.read_csv("F:/taobao_data.csv")
means=df['成交量'].groupby([df['位置'],df['卖家']])
means=means.mean()
print(means)

## Slide 24
数据分组与分割
1、数据分组
（2）列名用作分组
Pandas中可使用groupby()函数实现：按列名进行分组。
将数据按位置作为索引，按均值汇总所有的数值指标，具体代码如下：






运行程序，具体结果如下：


import pandas as pd
#从csv中读取数据，还可以读取html、txt等格式的文件
df=pd.read_csv("F:/taobao_data.csv")
group=df.groupby('位置')
group=group.mean()
print(group)

## Slide 25
数据分组与分割
1、数据分组
将数据按位置和卖家作为索引，按均值汇总所有的数值指标，具体代码如下：






运行程序，具体结果如下：


import pandas as pd
#从csv中读取数据，还可以读取html、txt等格式的文件
df=pd.read_csv("F:/taobao_data.csv")
groups=df.groupby(['位置','卖家'])
groups=groups.mean()
print(groups)

## Slide 26
数据分组与分割
1、数据分组
groupby的size()函数实现：可以返回一个含有各分组大小的Serise。
将数据按位置和卖家作为索引，汇总各分组大小的Serise，具体代码如下：






运行程序，具体结果如下：


import pandas as pd
#从csv中读取数据，还可以读取html、txt等格式的文件
df=pd.read_csv("F:/taobao_data.csv")
ser=df.groupby(['位置','卖家'])
ser=ser.size()
print(ser)

## Slide 27
数据分组与分割
2、数据分割
使用Pandas 切片可以对原有数据进行分割。
将数据进行分割，具体代码如下：






获取第30~39行数据，只保留位置和卖家两列，具体结果如下：


import pandas as pd
#从csv中读取数据，还可以读取html、txt等格式的文件
df=pd.read_csv("F:/taobao_data.csv")
df1=df[30:40][['位置','卖家']]
df2=df[80:90][['卖家','成交量']]
print(df1)

## Slide 28
数据分组与分割
2、数据分割
使用Pandas 切片可以对原有数据进行分割。
将数据进行分割，具体代码如下：






获取第30~39行数据，只保留位置和卖家两列，具体结果如下：


import pandas as pd
#从csv中读取数据，还可以读取html、txt等格式的文件
df=pd.read_csv("F:/taobao_data.csv")
df1=df[30:40][['位置','卖家']]
df2=df[80:90][['卖家','成交量']]
print(df1)

## Slide 29
1
数据分组与分割
数据清洗-pandas处理基础
数据读写、选择、整理和描述
2
3
数据合并和变形

## Slide 30
数据合并和变形
1、数据合并
Pandas 中有一些内置的合并数据集函数，具体如下：
pandas.merge()：依据一个或多个键将多个DataFrame连接起来，类似于数据库连接；
pandas.concat()：可以沿着一个轴将多个对象堆叠起来；
combine_first()：可以将重复数据编制在一起，用以填充另一个对象的缺失值。
（1）数据风格的DataFrame合并
数据库合并（merge）将一个或多个键将行连接起来，merge()具体语法如下：


主要参数解释：
                   left：DataFrame类型，左表；              
                   right：DataFrame类型，右表；



pandas.merge(left,right,how='inner',on=None,left_on=None,right_on=None,left_index=False,right_index=False,sort=False,suffixes=('_x', '_y'),copy=True,indicator=False,validate=None)

## Slide 31
数据合并和变形
主要参数解释：
how：连接方式，默认为inner，可设为inner，可设为outer/left/right；
                           left：仅保留左表的键；
                           right：仅保留右表的键；
                           outer:两表的键取并集；
                           inner:两表的键取交集。
                  on：用来对齐的那一列的名字，用到这个参数的时候一定要保证左表和右表用来对齐的那一列都有相同的列名；
                   left_index：将left行索引用作连接键；
                   right_index:将right行索引用作连接键。
pandas.merge(left,right,how='inner',on=None,left_on=None,right_on=None,left_index=False,right_index=False,sort=False,suffixes=('_x', '_y'),copy=True,indicator=False,validate=None)

## Slide 32
数据合并和变形
1、数据合并
使用inner连接方式合并数据，具体代码如下：







获取卖家相同的列名，具体结果如下：


import pandas as pd
#从csv中读取数据，还可以读取html、txt等格式的文件
df=pd.read_csv("F:/taobao_data.csv")
df1=df[30:40][['位置','卖家']]
df2=df[80:90][['卖家','成交量']]
df3=pd.merge(df1,df2,on='卖家')
print(df3)

## Slide 33
数据合并和变形
1、数据合并
使用left连接方式合并数据，具体代码如下：







运行运行结果如下：


import pandas as pd
#从csv中读取数据，还可以读取html、txt等格式的文件
df=pd.read_csv("F:/taobao_data.csv")
df1=df[30:40][['位置','卖家']]
df2=df[80:90][['卖家','成交量']]
df3=pd.merge(df1,df2,how='left')
print(df3)

## Slide 34
数据合并和变形
1、数据合并
使用right连接方式合并数据，具体代码如下：







运行运行结果如下：


import pandas as pd
#从csv中读取数据，还可以读取html、txt等格式的文件
df=pd.read_csv("F:/taobao_data.csv")
df1=df[30:40][['位置','卖家']]
df2=df[80:90][['卖家','成交量']]
df3=pd.merge(df1,df2,how='right',on='卖家')
print(df3)

## Slide 35
数据合并和变形
1、数据合并
（2）索引上的合并
Pandas中的merge()函数和join()函数可以实现合并索引。
merge()函数中的left_index=True或right_index=True，可以将索引作为连接键使用，具体代码如下：







运行运行结果如下：


import pandas as pd
#从csv中读取数据，还可以读取html、txt等格式的文件
df=pd.read_csv("F:/taobao_data.csv")
df1=df[:10][['位置','卖家']]
df2=df[:10][['卖家','成交量']]
df3=pd.merge(df1,df2,left_index=True,right_index=True)
print(df3)

## Slide 36
数据合并和变形
1、数据合并
（2）索引上的合并
join()函数可以更加方便地按照对象df的索引进行合并，具体语法如下：

主要参数解释：
                   other：DataFrame类型；              
                  how：连接方式，默认为inner，可设为inner，可设为outer/left/right；
                           left：仅保留左表的键；
                           right：仅保留右表的键；
                           outer:两表的键取并集；
                           inner:两表的键取交集。
                  on：用来对齐的那一列的名字，用到这个参数的时候一定要保证左表和右表用来对齐的那一列都有相同的列名；
DataFrame.join(other,on=None, how='left', lsuffix='', rsuffix='', sort=False)

## Slide 37
数据合并和变形
1、数据合并
（2）索引上的合并
使用join()函数进行索引合并，具体代码如下：







运行结果如下：


import pandas as pd
#从csv中读取数据，还可以读取html、txt等格式的文件
df=pd.read_csv("F:/taobao_data.csv")
df1=df[:10][['位置','卖家']]
df2=df[:10][['价格','成交量']]
df3=df1.join(df2)
print(df3)

## Slide 38
数据合并和变形
1、数据合并
（3）轴向连接
pandas.concat()函数可以合并多组数据，具体语法如下：



主要参数解释：
                   objs：Series，DataFrame或Panel对象的序列或映射。       
                  axis：{0,1，...}，默认为0，沿着连接的轴。0是按列轴方向合并行数据，1是按行轴方向合并列数据。 
                  join：{'inner'，'outer'}，默认为“outer”。如何处理其他轴上的索引。outer为联合和inner为交集。                   
pandas.concat(objs, axis=0, join='outer', join_axes=None, ignore_index=False,
          keys=None, levels=None, names=None, verify_integrity=False,
          copy=True)

## Slide 39
数据合并和变形
1、数据合并
（3）轴向连接
使用concat()函数连接单列数据，并且设定axis=0，具体代码如下：







运行结果如下：


import pandas as pd
#从csv中读取数据，还可以读取html、txt等格式的文件
df=pd.read_csv("F:/taobao_data.csv")
df1=df[:5]['宝贝']
df2=df[5:10]['宝贝']
df3=df[10:15]['宝贝']
df4=pd.concat([df1,df2,df3])
print(df4)

## Slide 40
数据合并和变形
1、数据合并
（3）轴向连接
使用concat()函数连接单列数据，并且设定axis=1，具体代码如下：







运行结果如下：


import pandas as pd
#从csv中读取数据，还可以读取html、txt等格式的文件
df=pd.read_csv("F:/taobao_data.csv")
df1=df[:5]['宝贝']
df2=df[:5]['价格']
df3=df[:5]['成交量']
df4=pd.concat([df1,df2,df3],axis=1)
print(df4)

## Slide 41
数据合并和变形
1、数据合并
（3）轴向连接
使用concat()函数连接多列数据，并且设定axis=0，具体代码如下：






运行结果如下：


import pandas as pd
#从csv中读取数据，还可以读取html、txt等格式的文件
df=pd.read_csv("F:/taobao_data.csv")
df1=df[:5][['宝贝','卖家']]
df2=df[:5][['价格','成交量']]
df3=pd.concat([df1,df2])
print(df3)

## Slide 42
数据合并和变形
1、数据合并
（3）轴向连接
使用concat()函数连接多列数据，并且设定axis=1，具体代码如下：






运行结果如下：


import pandas as pd
#从csv中读取数据，还可以读取html、txt等格式的文件
df=pd.read_csv("F:/taobao_data.csv")
df1=df[:5][['宝贝','卖家']]
df2=df[:5][['价格','成交量']]
df3=pd.concat([df1,df2],axis=1)
print(df3)

## Slide 43
数据合并和变形
1、数据合并
（3）轴向连接
使用concat()函数连接多列数据，并且设定axis=0，具体代码如下：






运行结果如下：


import pandas as pd
#从csv中读取数据，还可以读取html、txt等格式的文件
df=pd.read_csv("F:/taobao_data.csv")
df1=df[:5][['宝贝','卖家']]
df2=df[:5][['价格','成交量']]
df3=pd.concat([df1,df2])
print(df3)

## Slide 44
数据合并和变形
2、数据变形
（1）重塑层次化索引
针对DataFrame，Pandas有内置函数：
stack()函数：即“堆叠”，作用是将列旋转到行；
unstack()函数：即stack()的反操作，将行旋转到列。
使用stack()函数将天气数据进行变形，将DataFrame转化成一个层次化索引的Series，具体代码如下：






运行结果如下：

import pandas as pd
#从csv中读取数据，还可以读取html、txt等格式的文件
data=pd.read_csv("F:/hz_weather.csv")
#把列转换为行，即形成一个层次化索引的Series
df=data.stack()
print(df)

## Slide 45
数据合并和变形
2、数据变形
（1）重塑层次化索引
使用unstack()函数将天气数据进行变形，将一个层次化索引的Series转化成DataFrame，具体代码如下





运行结果如下：

import pandas as pd
#从csv中读取数据，还可以读取html、txt等格式的文件
data=pd.read_csv("F:/hz_weather.csv")
#把列转换为行，即形成一个层次化索引的Series
df=data.stack()
#把行转为列，把层次化索引的Series转化为一个普通的DataFrame
df1=df.unstack()
print(df1)

## Slide 46
数据合并和变形
2、数据变形
（2）数据透视表
透视表是一种可以对数据动态排布并且分类汇总的表格格式。
数据透视表是一种类似于Groupby的操作方法，常见于Excel与类似的表格应用中。数据透视表将每一列作为输入，输出将数据不断细分为多个维度累计信息的二维数据表。
数据透视表相比与Groupby操作更像是一种多维的GroupBy累计操作。虽然可以分割-应用-组合，但是分割和组合不是发生在一维索引上，而是在二维网络上（行和列同时分组）。
Pandas中的pivot_table()可以实现透视表功能，该方法具体以下特点：
灵活性高，可以随意定制分析计算要求；
脉络清晰，易于理解数据；
操作性强，报表神器。

## Slide 47
数据合并和变形
2、数据变形
(2)数据透视表
pandas.pivot_table()函数具体语法如下：


主要参数解释：
                   data : 创建透视表的DataFrame；          
                  values : 要聚合的值，默认聚合所有数据列；
                   index : 要聚合的index；
                   columns : 要聚合的columns，类似Index可以设置列层次字段，不是一个必要参数，作为一种分割数据的可选方式；
                   aggfunc :聚合的方式，default numpy.mean，可以是任何对groupby有效的函数。


pandas.pivot_table(data, values=None, index=None, columns=None, aggfunc='mean', fill_value=None, margins=False, dropna=True, margins_name='All')

## Slide 48
数据合并和变形
2、数据变形
（2）数据透视表
天气数据中以最高气温为数据域，天气为行、风向为列做数据视图，具体代码如下：







运行结果如下：


import pandas as pd
#从csv中读取数据，还可以读取html、txt等格式的文件
data=pd.read_csv("F:/hz_weather.csv")
df=data.set_index('日期')
#以最高气温为数据域，天气为行、风向为列做数据视图
df1=pd.pivot_table(df,values=['最高气温'],index=['天气'],columns=['风向'])
print(df1)

## Slide 49
小结
pandas数据选择的思路与常见方法
Pandas数据合并的思路和常见方法

## Slide 50
