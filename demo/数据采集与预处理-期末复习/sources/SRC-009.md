## Slide 1
爬虫框架-scrapy的数据处理

## Slide 2
复习
Scrpay框架包含哪些组件？
Scrapy项目包含哪些文件？每个文件的作用是什么？



## Slide 3
1、理解Scrapy构架中的数据输出
2、能编写pipline的python类进行数据处理
3、掌握将item数据存储到mysql中
4、能学会利用ImagesPipeline类爬取图片
教学目标

## Slide 4
1
Scrapy数据存储到Mysql
爬虫框架-scrapy的数据处理
Scrapy的数据输出
2
3
  scrapy图片下载器爬虫

## Slide 5
Scrapy的数据输出
scrapy可将数据输出为文本格式或存储至数据库。支持的常用文本格式有CSV、JSON、XML等，默认情况下不支持直接输出为Excel。

1、输出为文本
在Scrapy中，负责导出数据的组件被称为Exporter（导出器），Scrapy导出器提供了对数据格式的扩展。Scrapy内部实现了多个导出器，每个导出器实现一种数据格式的导出，支持的常用的数据格式有JSON 、CSV、XML。在大多数情况下，使用Scrapy内部提供的导出器就足够了。
在运行scrapy crawl命令时，可以分别使用参数-o和-t指定导出文件路径以及导出数据格式。-t参数也可省略，Scrapy框架会根据文件的扩展名推断文件格式。参考代码如下。



## Slide 6
Scrapy的数据输出
仔细观察导出的文件列名不是按照items.py文件定义的顺序进行输出的。如果需要控制列的输出顺序，可以在settings.py文件中通过参数FEED_EXPORT_FIELDS进行配置。参数FEED_EXPORT_FIELDS的类型为列表。参考配置如下。
    #指定输出字段列表，会按先后顺序输出。注意列表未出现的列不会输出
    FEED_EXPORT_FIELDS=["title","house","address"]

在保存的csv文件如果用excel打开，会发现中文乱码，解决的思路是：
1、在seting.py里设置 FEED_EXPORT_ENCODING = 'gb18030'   或者   'utf-8'   或者  'utf-8-sig'。
2、在scrapy crawl命令后面增加参数-s指定编码，如下：
scrapy crawl shellNews -o s1.csv -s FEED_EXPORT_ENCODING='utf-8-sig'


## Slide 7
Scrapy的数据输出
2、其他格式
 输出文本格式数据，除了使用Exporter组件输出外，也可编写pipelines实现对其他数据格式的支持，如输出为Excel格式。可以使用openpyxl来实现。
openpyxl是一个开源项目，openpyxl模块是一个读写Excel 2010文档的Python库。penpyxl是一款比较综合的工具，不仅能够同时读取和修改Excel文档，而且可以对Excel文件内单元格进行详细设置，包括单元格样式等内容，甚至还支持图表插入、打印设置等内容。openpyxl安装命令如下。


## Slide 8
Scrapy的数据输出
打开pipelines.py文件，编写类编写ExcelPipeline。其中open_spider方法实现excel的创建以及表头的设置，process_item方法实现数据的写入操作。参考如下代码。

## Slide 9
1
Scrapy数据存储到Mysql
爬虫框架-scrapy的数据处理
Scrapy的数据输出
2
3
  scrapy图片下载器爬虫

## Slide 10
Scrapy数据存储到Mysql
当Item在Spider中被收集之后，它将会被传递到Item Pipeline又称之为管道,顾名思义就是对数据的过滤处理, 这些Item Pipeline组件按定义的顺序处理Item。每个Item Pipeline都是实现了简单方法的Python类，其主要的作用包括：
HTML数据清洗
爬取的数据验证（检查item包含某些字段）
查重并丢弃
将爬取结果保存到数据库或者文件中
如果将数据存储至数据库，既可将数据库存储至mysql、SQLServer等关系型数据库，也可可将数据存储至Mangodb等非关系型数据库，但都需要下载第三方库。

## Slide 11
Scrapy数据存储到Mysql
爬取的数据如果想要直接存储至mysql数据库，须提前安装MySQL数据库和第三方库PyMySQL,具体安装方法参见前面数据存储部分。
Item Pipeline组件以独立的Python类进行组织，存放在pipelines.py文件中。其中process_item()方法必须实现
import somethingclass SomethingPipeline(object):    def process_item(self, item, spider):        # item (Item 对象) – 被爬取的item        # spider (Spider 对象) – 爬取该item的spider        # 这个方法必须实现，每个item pipeline组件都需要调用该方法，        # 这个方法必须返回一个 Item 对象，被丢弃的item将不会被之后的pipeline组件所处理。        return item

## Slide 12
Scrapy数据存储到Mysql
 def __init__(self):        # 可选实现，做参数初始化等        # doing something
def open_spider(self, spider):    # spider (Spider 对象) – 被开启的spider    # 可选实现，当spider被开启时，这个方法被调用。def close_spider(self, spider):    # spider (Spider 对象) – 被关闭的spider    # 可选实现，当spider被关闭时，这个方法被调用
Item Pipeline组件以独立的Python类进行组织，存放在pipelines.py文件中。还有3个可选的方法，用来进行参数初始化或者对spider打开或关闭时调用的函数

## Slide 13
Scrapy数据存储到Mysql
一般实现数据存储分为建立链接、操纵每一个item数据、提交事务和释放资源三个步骤，需要分别实现open_spider、process_item、close_spider三个方法。
class MySQLPipeline:      #作用：读取配置文件，初始化连接以及游标。
      #时机：开始爬取数据之前被调用      def open_spider(self, spider):            pass      #作用：处理数据，数据库的增删改查操作      def process_item(self, item, spider):           pass      #作用：提交数据，释放资源，
      #时机：process_item执行完后被调用
      def close_spider(self, spider):            pass

## Slide 14
Scrapy数据存储到Mysql
案例1：以贝壳新房案例讲解数据存储至MySQL数据库的实现方法。在讲解前需要根据爬取的数据项，在MySQL数据库中创建表HouseInfo。HouseInfo表结构如表所示。

## Slide 15
Scrapy数据存储到Mysql
案例1：以贝壳新房案例讲解数据存储至MySQL数据库的实现方法。 
【实现步骤】
1、pipelines.py文件中编写MySQLPipeline。需要实现open_spider方法，此方法在开始爬取数据之前被调用，用于初始化数据库链接。
为提高代码的通用性，将数据库的链接配置存储在settings.py文件中。配置项包括主机名、端口、数据库名称、连接用户、密码。配置名称见名知意即可，配置项可放在settings.py文件中的任意位置。参考配置项如下。
MYSQL_DB_HOST="服务器IP"MYSQL_DB_PORT=3306  #端口MYSQL_DB_NAME="数据库名称"MYSQL_DB_USER="用户名"MYSQL_DB_PASSWORD="密码"

## Slide 16
Scrapy数据存储到Mysql
案例1：以贝壳新房案例讲解数据存储至MySQL数据库的实现方法。 
【实现步骤】
2、配置好后，使用spider.settings.get方法从settings.py文件中读取连接数据库需要的配置项，第一个参数为配置项的名称，第二个参数为缺省配置项，根据自己的环境情况自行调整。读取全部配置项后，使用pymysql.connect方法初始化数据库链接，并使用db_conn.cursor()方法打开游标，为下一步操纵数据做准备。参考代码如下

## Slide 17
Scrapy数据存储到Mysql
案例1：以贝壳新房案例讲解数据存储至MySQL数据库的实现方法。 
【实现步骤】
3、实现process_item方法，此方法用于操纵数据，实现增删改查操作。当前贝壳新房案例，只需要从item中读取数据，生成insert into语句即可。
def process_item(self, item, spider):       values = (          item["house"],          item["address"],          item["price"],          item["total"],) #与占位符%s对应的数据
#sql语句，数据部分使用占位符%s代替       sql = "insert into HouseInfo(house,address,price,total) values(%s,%s,%s,%s)"       self.db_cur.execute(sql, values) #执行SQL语句       return item

## Slide 18
Scrapy数据存储到Mysql
案例1：以贝壳新房案例讲解数据存储至MySQL数据库的实现方法。 
【实现步骤】
4、在process_item方法中并没有调用连接对象的commit方法，也就意味着此时数据并没有实际写入数据库。如果每插入一条数据都调用一次commit方法，会严重降低程序执行效率。因此事务提交在close_spider方法中完成，该方法的作用为事务提交和资源释放。参考代码如下。

def close_spider(self,spider):        self.db_conn.commit() #提交事务        self.db_cur.close()  #关闭游标        self.db_conn.close() #关闭数据库连接

## Slide 19
Scrapy数据存储到Mysql
案例1：以贝壳新房案例讲解数据存储至MySQL数据库的实现方法。 
【实现步骤】
5、完成MySQLPipeline的编写后，需要在settings.py文件中进行配置，才能实现将爬虫数据数据存储至MySQL数据库。在settings.py文件中找到ITEM_PIPELINES，修改配置项。参考配置如下。
。参考代码如下。

ITEM_PIPELINES = {       'house.pipelines.MySQLPipeline': 300,    }  #格式为“项目名.pipelines.pipeline类名”
一个爬虫项目中可以编写多个pipeline,通过字典形式进行配置，参数300为权重值，数字越小优先级越高。


## Slide 20
1
Scrapy数据存储到Mysql
爬虫框架-scrapy的数据处理
Scrapy的数据输出
2
3
 scrapy图片下载器爬虫

## Slide 21
scrapy图片下载器爬虫
ImagesPipeline是scrapy自带的类，用来处理图片（爬取时将图片下载到本地）用的。
优势：
将下载图片转换成通用的JPG和RGB格式
避免重复下载
缩略图生成
图片大小过滤
异步下载

## Slide 22
scrapy图片下载器爬虫
工作流程：
爬取一个Item，将图片的URLs放入image_urls字段
从Spider返回的Item，传递到Item Pipeline
当Item传递到ImagePipeline，将调用Scrapy 调度器和下载器完成image_urls中的url的调度和下载。
图片下载成功结束后，图片下载路径、url和校验和等信息会被填充到images字段中。
实现方式：
自定义pipeline，优势在于可以重写ImagePipeline类中的实现方法，可以根据情况对照片进行分类；
直接使用ImagePipeline类，简单但不够灵活；所有的图片都是保存在full文件夹下，不能进行分类


## Slide 23
scrapy图片下载器爬虫
ImagesPipeline是Scrapy中的一个pipe管道组件的一个插件模块，封装了对图片爬取的一些处理，是Scrapy给出的图片处理方案，可以避免我们重复造轮子，我们只要拿到图片链接丢给这个管道，其他的不用我们管，包括图片的命名和后缀名处理，图片存储地址（需要修改默认值），图片长宽处理等。
在继承ImagesPipeline的子类中，作为重要的是要重载get_media_requests(self, item, info)和item_completed(self, results, item, info)这两个函数：
get_media_requests(self,item, info)：
ImagePipeline根据image_urls中指定的url进行爬取，可以通过get_media_requests为每个url生成一个Request。如：

def get_media_requests(self, item, info): #请求下载指定item对象数据    yield Request(item["image_urls"])

## Slide 24
scrapy图片下载器爬虫
item_completed(self, results, item, info)：
   图片下载完毕后，处理结果会以二元组的方式返回给item_completed()函数的results 中。这个二元组定义如下：
# results - 下载好的资源参数([(True, {'url':'https://img.mukewang.com/5861d2500001d39406000338-240-135.jpg',#  'path': 'full/6922b98c7acde37f0b570650844e2e660b82991a.jpg',#   'checksum': '037f4f643599f3e7870225798ece845b', 'status': 'downloaded'})])
其中，第一个元素表示图片是否下载成功；第二个元素是一个字典。如：

def item_completed(self, results, item, info): 
     image_paths = [x['path'] for ok, x in results if ok] 
if not image_paths: 
	raise DropItem("Item contains no images") 
item['image_paths'] = image_paths 
return item

## Slide 25
scrapy图片下载器爬虫
案例2：贝壳新房案例要求爬取某个城市挂牌出售的全部楼盘信息。爬取信息已经包括楼盘名称、地址、每平米均价、总房价，增加一个楼盘图片信息。目标网站url地址为https://cq.fang.ke.com/loupan。

## Slide 26
scrapy图片下载器爬虫
【任务实现】
1、在items.py中增加图片地址和路径的字段


## Slide 27
scrapy图片下载器爬虫
【任务实现】
2、在pipeline文件中编写继承ImagesPipeLine的图片处理类中的get_media_requests(self, item, info)和item_completed(self, results, item, info)这两个函数


## Slide 28
scrapy图片下载器爬虫
【任务实现】
3、修改配置文件settings.py 。
（1）pipeline中的类需要在在settings.py里打开才能使用。
在settings.py文件中找到ITEM_PIPELINES，修改配置项。参考配置如下。




（2）IMAGES_STORE是图片存储的路径.需要在settings.py文件中进行配置




## Slide 29
小结及作业
Pipeline编写思路
作业：
模仿课堂案例：58同城二手房案例要求爬取重庆挂牌出售的全部二手房信息信息。爬取信息包括卖点、楼盘、楼盘地址、房屋户型、楼层、建筑年代、每平单价、房屋总价。目标网站url地址为https://cq.58.com/ershoufang/。

## Slide 30
