## Slide 1
动态网页的爬取

## Slide 2
复习
动态网页的判定方法？

## Slide 3
动态网页的爬取方法
动态网页的爬取方法一般分为逆向分析法和模拟法
逆向分析法难度较高，通过拦截网站发送的请求，找出真正的请求地址，要求爬虫爱好者熟悉前端特别是JavaScript相关技术。
模拟法是使用第三方库如Selenium模拟浏览器的行为，解决页面加载和渲染的问题。


## Slide 4
动态渲染页面爬取
网络爬虫时，网页不止有静态页面还有动态页面，动态页面主要由JavaScript动态渲染，网络爬虫经常遇见爬取JavaScript动态渲染的页面。


动态渲染页面爬取，就是模拟浏览器的运行方式，可以做到在浏览器中看到是什么内容爬取的源码就是相应的内容，实现了可见即可爬。


如何爬取JavaScript动态渲染的页面？
动态渲染页面爬取

## Slide 5
1、了解selenim的安装部署过程
2、理解selenim的基本使用过程
3、学会利用css选择器定位元素
4、熟悉常用的操纵元素的方法
教学目标

## Slide 6
1
selenium的基本使用
动态网页数据采集– selenium基础
selenium的安装部署
2
3
Selenium的元素定位
4
Selenium操纵元素

## Slide 7
selenium的安装部署
安装部署
Selenium是一个 自动化测试工具，利用它可以驱动浏览器执行特定的动作，如点击、下拉等操作， 同时还可以获取浏览器当前呈现的页面的源代码，做到可见即可爬。
Selenium库安装如下：


由于pip使用的源多为国外镜像，网速较慢。可以使用pip -i参数，指定国内镜像源。以下使用的https://pypi.tuna.tsinghua.edu.cn/simple为清华镜像。



pip install selenium -i https://pypi.tuna.tsinghua.edu.cn/simple

## Slide 8
selenium的安装部署
浏览器驱动的下载安装
         浏览器驱动也是一个独立的程序，是由浏览器厂商提供的，不同的浏览器需要不同的浏览器驱动。比如Chrome浏览器和火狐浏览器有各自不同的驱动程序。
        浏览器驱动在接收到我们自动化程序发送的界面操作请求后，会转发请求给浏览器， 让浏览器去执行对应的自动化操作。浏览器执行完操作后，会将自动化的结果返回给浏览器驱动，浏览器驱动再通过HTTP响应的消息返回给我们的自动化程序的客户端库。自动化程序的客户端库接收到响应后，将结果转化为数据对象返回给程序代码。
       在下载Chrome浏览器驱动前，首先确定Chrome浏览器的版本。点击Chrome浏览器“自定义及控制Goole Chrome”按钮，选择“帮助”、“关于Google Chrome(G)”，查看浏览器的实际版本号。如图所示。




## Slide 9
浏览器驱动的下载安装
Chrome浏览器驱动的下载地址为http://npm.taobao.org/mirrors/chromedriver，也可百度搜索“ChromeDriver”关键字进行下载。按照Chrome的版本号以及操作系统，选择不同的版本下载。如图所示。





下载完成后解压缩，将ChromeDriver.exe拷贝到指定目录，后续编写代码要指定驱动所在目录。
selenium的安装部署

## Slide 10
1
selenium的基本使用
动态网页数据采集– selenium基础
selenium的安装部署
2
3
Selenium的元素定位
4
Selenium操纵元素

## Slide 11
声明浏览器对象
Selenium支持多个浏览器，比如：Chrome、Firefox、Edge等，还可以支持Android、BlackBerry等手机段浏览器。另外也支持无界面浏览器PhantomJS。
具体的初始化方式如下：






其中，executable_path表示：浏览器驱动器存放位置（上一步驱动器的位置）。
以上步骤实现了浏览器对象的初始化，并将其赋值为browser对象。

selenium的基本使用
from selenium import webdriver
browser = webdriver.Chrome(executable_path=path)
browser=webdriver.Firefox(executable_path=path)
browser=webdriver.Edge(executable_path=path)
browser=webdriver.PhantomJS(executable_path=path)
browser=webdriver.Safari(executable_path=path)

## Slide 12
访问页面
Selenium使用get()方法请求网页，具体的语法如下：

访问页面的实现方式如下：






运行程序后，弹出了Chrome浏览器并且自动访问了百度，然后输出百度网页的源代码，最后关闭浏览器。






selenium的基本使用
from selenium import  webdriver #导入webdriverimport  timepath="D:/安装软件/chromedriver_win32/ChromeDriver.exe“  browser = webdriver.Chrome(executable_path=path)#获取Chrome驱动实例browser.get('https://www.baidu.com/')  #打开百度time.sleep(3)  #等待3s，防止网页未加载完成 print(browser.page_source)  #输出网页的源代码
browser.close ()#关闭浏览器
browser.get(url)

## Slide 13
1
selenium的基本使用
动态网页数据采集– selenium基础
selenium的安装部署
2
3
Selenium的元素定位
4
Selenium操纵元素

## Slide 14
Selenium的元素定位
查找节点
	       webdriver提供八种元素定位方法，主要通过元素属性和层级关系定位，有两套定位方法。
              第一套是使用webdriver提供的find_element_by_*的八种元素定位方法，from selenium import webdriver。一个是find_element_by_方式，这是定位单个元素的。一个是find_elements_by_方式，这是用来定位多个元素的。


## Slide 15
Selenium的元素定位
查找节点
	       webdriver提供八种元素定位方法，主要通过元素属性和层级关系定位，有两套定位方法。
第二套通过统一调用find_element(By.xx,"yyy")方法，通过By声明定位方法，传入对应定位参数） 导入By类 ,from selenium.webdirver.common.by import By。
          find_element()只会查找页面符合条件的第一个节点，并返回；但是定位不到元素则会报错。find_elements()查找多个元素并且返回一个列表，列表里的元素全是WebElement节点对象；当定位不到元素时不会报错，会返回一个空列表。
1.Selenium提供了通用方法find_element()，需要传入两个参数：查找方式By和值；
2.find_element_by_id()方法与find_element(By.ID,id)得到的结果完全一致。

## Slide 16
Selenium的元素定位

## Slide 17
Selenium的元素定位
以下以百度首页为例进行基本案例讲解。CSS选择器的基本使用方法要求掌握，简要回顾下。
Id选择器使用#，如“#u1”，定位id为u1的元素;
类选择器使用“.”，如“.mnav”，定位所有class为mnav的元素;
元素选择器直接使用标签名，如“div”，定位所有的div;
组合选择器，以上多种元素选择方式组合在一起，是使用频率最高的一类选择器。如“#u1 .pf”,定位id为u1的元素下的所有class为pf的元素；“#u1>.pf”,定位id为u1的元素下的class为pf的元素,并且要求class为pf的元素是u1的直接子级。

	

## Slide 18
Selenium的元素定位
百度首页查找节点

	
element=browser.find_element_by_css_selector(“#s-top-left”) #获得1个元素print(element.get_attribute(“class”)) #获得元素的属性值print(element.get_attribute(“outerHTML”)) #元素的js属性
print(element.text)   #元素的文本

## Slide 19
Selenium的元素定位
通过css选择器获得多个元素的集合





	此处，得到的内容是列表类型，列表中每个节点都是WebElement类型。每个网页元素可以直接访问id，标签名，位置和大小等属性

结果：

search=browser.find_element_by_id('srch')
print('the id of node:',search.id)
print('the tag of node:',search.tag_name)
print('the location of node:',search.location)
print('the size of node:',search.size)


## Slide 20
Selenium的元素定位
获取节点信息
Selenium提供了多种选择节点信息的方法，返回的是WebElement类型，常用的方法如下：

## Slide 21
1
selenium的基本使用
动态网页数据采集– selenium基础
selenium的安装部署
2
3
Selenium的元素定位
4
Selenium操纵元素

## Slide 22
Selenium操纵元素
动作链
Selenium可驱动浏览器执行其他操作，这些操作没有特定的执行对象，比如：鼠标拖拽、键盘按键等，此类操作称为动作链。
Selenium库提供了Actionchains模块，该模块专门处理动作链，比如：鼠标移动，鼠标按钮操作，按键、上下文菜单（鼠标右键）交互等。
Actionchains提供的常用方法如下：


## Slide 23
节点交互
Selenium可驱动浏览器执行一些操作，即可以让浏览器模拟执行一些动作，常见的操作及方法如下：
输入文字：使用send_keys()方法实现
清空文字：使用clear()方法实现
点击元素或按钮：使用click()方法实现

Selenium操纵元素
在百度首页上进行节点交互，点击元素是模拟用户鼠标点击操作，如点击新闻链接。find_element_by_link_text函数进行定位后通过“click()”实现单击操作。参考代码如下。


## Slide 24
节点交互
输入文字：使用send_keys()方法实现
Selenium操纵元素
在百度首页上进行节点交互，将元素选择、在页面中输入数据、点击元素综合使用。实现打开百度首页，输入“python”，然后点击“百度一下”获取，获取页面检索结果。参考代码如下。


## Slide 25
使用selenium实现百度首页模拟登录，然后获取你所在城市的今日天气。百度首页在没有登录前，不会显示你所在城市的天气，所以必须先实现模拟登录。如用户没有百度的用户名和密码，需要先注册。
【任务分析】
百度登录设计安全性较高，如果使用逆向分析法实现模拟登录难度较大。使用selenium可以很好的实现模拟登录的任务。
案例精讲-百度首页登录
【任务描述】

## Slide 26
模拟登录核心任务是确定需要操纵元素的CSS选择器，然后通过click()和sendkeys()函数操纵元素，进而实现模拟登录功能。登录完成后通过页面解析实现今日天气的获取。

案例精讲-百度首页登录
【任务分析】

## Slide 27
案例精讲-百度首页登录
【任务实现】
1、使用selenium实现模拟登录，核心是确定选择器。当然为了保证元素能够加载完成中间需要增加等待操作。



2、按照操作步骤依次点击登录链接、点击用户名登录、输入用户名和密码、点击登录按钮操作。选择器的使用根据类型进行判断，对于超级链接优先选择find_element_by_link_text函数，通过链接文本进行定位；对于非超级链接优先使用find_element_by_id函数，根据id属性值进行定位，如果没有id属性建议直接使用find_element_by_css_selector。




## Slide 28
案例精讲-百度首页登录
【任务实现】
2.1使用开发者工具分析工具分析“登录”链接的CSS样式，然后使用click()实现链接点击

2.2 分析“用户名登录”链接，此处不能使用driver.find_element_by_link_text("用户名登录").click(),原因如下


## Slide 29
2.3  自动输入用户名和密码，使用开发者工具分析用户名、密码以及登录按钮的CSS标识

案例精讲-百度首页登录
【任务实现】


## Slide 30
案例精讲-百度首页登录
【任务实现】
3、解析selenium访问的页面，获得天气信息标签。可以用浏览器直接查找天气的盒子.show-weather，也可以用xpah或beatifulsoap对获得的页面进行解析，以下是两种实现方法的代码。


## Slide 31
小结
Selenium查找网页元素，id，css选择器
熟悉常用的操纵元素的方法




## Slide 32
